FULL SITE — improved search snippets (titles + descriptions)
=============================================================
Drag everything into the repo ROOT and commit (merges; images kept).

This round: every page's meta description trimmed to ~140-155 chars so Google
shows it in full (they were being cut off mid-sentence at ~160). Title
separators fixed from ", " to " | " on the homepage, coloring-book and
work-with-us pages, and a couple of over-long titles shortened. Keywords kept.

Also includes the centered-layout fix, deepened pages, share button, City
Museum widget and lodging-CTA fixes from the recent rounds, so this one upload
brings the whole site current.
